nc: 1
p2_offset_regression: false
scale: n
scales: {n: [0.33, 0.25, 1024]}
backbone:
  - [-1, 1, Conv, [64, 3, 2]]
  - [-1, 1, Conv, [128, 3, 2]]
  - [-1, 3, C2f, [128, true]] # 2 (Backbone P2, 128 channels)
  - [-1, 1, Conv, [256, 3, 2]]
  - [-1, 6, C2f, [256, true]]
  - [-1, 1, Conv, [512, 3, 2]]
  - [-1, 6, C2f, [512, true]]
  - [-1, 1, Conv, [1024, 3, 2]]
  - [-1, 3, C2f, [1024, true]]
  - [-1, 1, SPPF, [1024, 5]]
head:
  - [-1, 1, nn.Upsample, [null, 2, nearest]] # 10 (P5 -> P4)
  - [[-1, 6], 1, Concat, [1]] # 11
  - [-1, 3, C2f, [512]] # 12
  - [-1, 1, nn.Upsample, [null, 2, nearest]] # 13 (P4 -> P3)
  - [[-1, 4], 1, Concat, [1]] # 14
  - [-1, 3, C2f, [256]] # 15
  - [-1, 1, nn.Upsample, [null, 2, nearest]] # 16 (P3 -> P2 upsampled)
  - [2, 1, KVCompressedAttention, [128, 4, 8, "group_weight"]] # 17 (KVCA on Backbone P2)
  - [[16, 17], 1, Concat, [1]] # 18 (Concat P3 upsampled and KVCA-Backbone P2)
  - [-1, 3, C2f, [128]] # 19 (Fused P2)
  - [-1, 1, ChannelAttention, []] # 20 (GAP after fusion)
  - [[20], 1, Detect, [nc]] # 21 (Detect head)
