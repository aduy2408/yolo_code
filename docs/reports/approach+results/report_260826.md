# Báo cáo thí nghiệm augmentation LEVIR-Ship

**Ngày:** 26/08/2026  
**Workflow:** Marimo training workflow  
**Model:** YOLOv8n P2, FPN-only CBAM channel-only configuration  
**Loss/target:** GAP + FTAL legacy, `factorized_tal_p2_only=true`  
**Dataset:** LEVIR-Ship, test split 788 ảnh  
**Mục tiêu:** đánh giá các augmentation xử lý partial visibility, degradation và Mosaic mà không thêm augmentation primitive mới ngoài nhóm đã triển khai.

---

## 1. Training protocol

Các run dùng cùng protocol để giữ ablation có ý nghĩa:

- `imgsz=512`, `batch=8`, `epochs=100`, `patience=0`.
- `pretrained=yolov8n.pt`, `optimizer=auto`, `device=cuda`, `workers=4`.
- `seed=42` cho các run chính; `seed=43,44` được thêm cho các variant cần kiểm tra độ ổn định.
- `deterministic=true`.
- `factorized_tal_target=true`, `factorized_tal_mode=legacy`.
- `factorized_tal_tau=0.75`, `factorized_tal_kappa=1.5`, `factorized_tal_lambda=0.5`.
- `factorized_tal_p2_only=true`, warmup epoch 5–15, `s_max=32`.
- `ggcf_refine=false`; không dùng GGCF refinement.
- Test evaluation dùng explicit NMS IoU `0.5`.
- Canonical baseline tham chiếu trong `docs/current_best.md`:
  - Test mAP50: **0.8283**.
  - Test mAP50-95: **0.3161**.

Các metric dưới đây lấy từ `evaluation_metrics.json`, không phải inspection thủ công từ log training.

---

## 2. Augmentation methods

### 2.1 Independent transforms

#### RandomViewport

Pipeline:

```text
image + boxes
→ isotropic resize, scale ∈ [0.80, 1.25]
→ translation ∈ ±0.15 × output size
→ crop/pad về 512×512, fill=114
→ clip bbox
→ visibility = clipped_area / preclip_area
→ drop nếu visibility < 0.40 hoặc box side < 2 px
```

Không chọn theo class hoặc kích thước object. Core bbox logic được kiểm tra với identity, translation, partial clipping và complete disappearance.

#### BBoxPartialOcclusion

Pipeline:

```text
mỗi ảnh với p=0.30
→ mỗi GT có object probability=0.50
→ che một cạnh ngẫu nhiên
→ occlusion ratio ∈ [0.10, 0.35]
→ giữ nguyên bbox label
→ fill bằng local mean từ ring quanh bbox
```

Mục tiêu là buộc detector dự đoán full extent từ partial evidence. `mode=edge` là mode duy nhất được hỗ trợ trong implementation hiện tại.

#### ResolutionDegrade

Pipeline:

```text
ảnh hiện tại
→ downsample với scale ∈ [0.65, 1.0], INTER_AREA
→ upsample về kích thước đầu vào, INTER_LINEAR
```

Bbox không thay đổi.

### 2.2 Mosaic-compatible transforms

- `viewport_mosaic`: Mosaic → RandomViewport → HSV → Flip.
- `mosaic_occlusion`: Mosaic → RandomPerspective → BBoxPartialOcclusion → HSV → Flip.
- `mosaic_resolution_degrade`: Mosaic → RandomPerspective → ResolutionDegrade → HSV → Flip.
- `mosaic_occlusion_resolution`: Mosaic → RandomPerspective → Occlusion → ResolutionDegrade → HSV → Flip.
- `viewport_mosaic_occlusion`: Mosaic → RandomViewport → Occlusion → HSV → Flip.
- `viewport_mosaic_all`: Mosaic → RandomViewport → Occlusion → ResolutionDegrade → HSV → Flip.

Với `viewport_mosaic`, RandomViewport được ép `p=1.0` để luôn collapse canvas Mosaic 1024×1024 về input 512×512. Với các augmentation pixel-only, RandomPerspective vẫn được giữ để đảm bảo output Mosaic được đưa về kích thước training.

### 2.3 Late clean-tail schedule

Để kiểm tra giả thuyết “augment early, recover late”, thêm config `clean_tail_epoch=80`:

```text
epoch 1–80: augmentation được chỉ định
epoch 81–100: canonical Mosaic → RandomPerspective → HSV → Flip
```

Hai variant:

- `mosaic_resolution_degrade_tail`:
  - Epoch 1–80: Mosaic → RandomPerspective → ResolutionDegrade.
  - Epoch 81–100: canonical Mosaic → RandomPerspective.
- `viewport_mosaic_tail`:
  - Epoch 1–80: Mosaic → RandomViewport.
  - Epoch 81–100: canonical Mosaic → RandomPerspective.

Implementation được commit tại `9dce4ea` và sync vào marimo trước khi chạy.

---

## 3. Independent and Mosaic results, seed 42

| Variant | Pipeline family | mAP50 | mAP50-95 | AP75 |
|---|---|---:|---:|---:|
| `clean_control` | LetterBox → HSV → Flip | 0.7229 | 0.2805 | 0.1239 |
| `random_viewport` | LetterBox → RandomViewport → HSV → Flip | 0.8056 | 0.3088 | 0.1265 |
| `bbox_occlusion` | LetterBox → Occlusion → HSV → Flip | 0.7092 | 0.2628 | 0.1112 |
| `resolution_degrade` | LetterBox → ResolutionDegrade → HSV → Flip | 0.6794 | 0.2509 | 0.0874 |
| `viewport_occlusion` | LetterBox → Viewport → Occlusion → HSV → Flip | 0.8008 | 0.3018 | 0.1280 |
| `viewport_mosaic` | Mosaic → RandomViewport → HSV → Flip | 0.8106 | **0.3251** | 0.1381 |
| `mosaic_occlusion` | Mosaic → RP → Occlusion → HSV → Flip | 0.8108 | 0.3155 | 0.1308 |
| `mosaic_resolution_degrade` | Mosaic → RP → Resolution → HSV → Flip | 0.8105 | **0.3251** | **0.1544** |
| `mosaic_occlusion_resolution` | Mosaic → RP → Occlusion → Resolution → HSV → Flip | 0.8067 | 0.3025 | 0.1158 |
| `viewport_mosaic_occlusion` | Mosaic → Viewport → Occlusion → HSV → Flip | **0.8347** | 0.3201 | 0.1198 |
| `viewport_mosaic_all` | Mosaic → Viewport → Occlusion → Resolution → HSV → Flip | 0.8232 | 0.3124 | 0.1287 |

`viewport_mosaic_occlusion` đạt mAP50 cao nhất ở seed 42, nhưng cần kiểm tra đa seed trước khi kết luận.

---

## 4. Multi-seed stability checks

### 4.1 `mosaic_occlusion`

| Seed | mAP50 | mAP50-95 | AP75 | Precision | Recall |
|---:|---:|---:|---:|---:|---:|
| 42 | 0.810832 | 0.315525 | 0.130809 | 0.842404 | 0.752874 |
| 43 | 0.832918 | 0.309900 | 0.104749 | 0.831037 | 0.791477 |
| 44 | 0.836544 | 0.319422 | 0.120739 | 0.838847 | 0.778736 |
| **Mean ± sample SD** | **0.826764 ± 0.013917** | **0.314949 ± 0.004787** | **0.118766 ± 0.013142** | **0.837430 ± 0.005815** | **0.774362 ± 0.019670** |

So với canonical mAP50 `0.8283`, mean mAP50 thấp hơn khoảng `0.0015`. Đây là candidate hợp lý nhất trong nhóm augmentation bổ sung, nhưng chưa chứng minh được improvement.

### 4.2 `viewport_mosaic_occlusion`

| Seed | mAP50 | mAP50-95 | AP75 |
|---:|---:|---:|---:|
| 42 | 0.834658 | 0.320075 | 0.119828 |
| 43 | 0.797913 | 0.315527 | 0.120511 |
| 44 | 0.810829 | 0.317631 | 0.132229 |
| **Mean ± sample SD** | **0.814467 ± 0.018911** | **0.317744 ± 0.002278** | **0.124189 ± 0.006775** |

Variant này có seed 42 tốt nhưng rơi mạnh ở seed 43–44. Do đó không ổn định.

---

## 5. Late-tail results, seed 42

| Variant | Epoch 1–80 | Epoch 81–100 | mAP50 | mAP50-95 | AP75 | Precision | Recall |
|---|---|---|---:|---:|---:|---:|---:|
| `mosaic_resolution_degrade_tail` | Mosaic + RP + Resolution | Canonical Mosaic + RP | **0.813004** | **0.321783** | **0.139638** | 0.852608 | 0.770115 |
| `viewport_mosaic_tail` | Mosaic + Viewport | Canonical Mosaic + RP | 0.811878 | 0.317798 | 0.133418 | 0.827490 | 0.774425 |

Comparison with canonical baseline:

- `mosaic_resolution_degrade_tail`: mAP50 thấp hơn `0.0153`, nhưng mAP50-95 cao hơn `0.0057`.
- `viewport_mosaic_tail`: mAP50 thấp hơn `0.0164`, mAP50-95 cao hơn khoảng `0.0017`.

Comparison with always-on seed-42 variants:

- `mosaic_resolution_degrade_tail` so với `mosaic_resolution_degrade`:
  - mAP50: `0.8105 → 0.8130`, tăng `0.0025`.
  - mAP50-95: `0.3251 → 0.3218`, giảm `0.0033`.
  - AP75: `0.1544 → 0.1396`, giảm `0.0147`.
- `viewport_mosaic_tail` so với `viewport_mosaic`:
  - mAP50: `0.8106 → 0.8119`, tăng `0.0012`.
  - mAP50-95: `0.3251 → 0.3178`, giảm `0.0073`.
  - AP75: `0.1381 → 0.1334`, giảm `0.0047`.

Late tail có dấu hiệu recover một phần mAP50, nhưng không tạo improvement đồng thời trên mAP50, mAP50-95 và AP75.

---

## 6. Validation and artifact status

### Code and public interface

- `python train_all_levir_yolov8n_p2_augmentations.py --help`: **PASS**.
- `compileall` trên implementation, config và runner: **PASS**.
- `git diff --check`: **PASS**.
- New config `clean_tail_epoch` đã được đăng ký trong config type validation và default YAML.
- Commit implementation: `9dce4ea Add late clean-tail augmentation variants`.

### Transform edge cases

Đã chạy trong marimo environment, nơi có đầy đủ dependency:

- Negative crop origin với padding: **PASS**.
- Half-visible bbox, visibility `0.5`: **PASS**.
- Bbox biến mất hoàn toàn bị drop: **PASS**.
- Viewport identity giữ nguyên image và bbox: **PASS**.
- Occlusion giữ nguyên bbox label: **PASS**.
- Resolution degradation giữ shape và bbox: **PASS**.
- Local mean fill lấy từ ring quanh bbox: **PASS**.

Local workspace không có `numpy`, nên direct transform test được chạy ở marimo environment thay vì local Python environment.

### Real training integration

- Cả hai late-tail run chạy đủ **100/100 epoch** trên marimo.
- `args.yaml` xác nhận `seed=42`, `factorized_tal_mode=legacy`, `clean_tail_epoch=80` và đúng flags từng variant.
- `results.csv` và `evaluation_metrics.json` tồn tại cho cả hai.
- Không còn training process sau khi hoàn tất.

### Hugging Face artifacts

Repository artifact:

- [LEVIR augmentation dataset repo](https://huggingface.co/datasets/duyle2408/levir-yolov8n-p2-gap-factorized-tal-augmentations-legacy-mosaic-seed42)

Hai late-tail variant đã được upload bằng `HF_TOKEN` trong **global marimo kernel**, không phải `os.environ` của subprocess. Remote verification dùng đúng `repo_type="dataset"` và xác nhận cho mỗi variant có:

- `args.yaml`
- `results.csv`
- `evaluation_metrics.json`
- `upload_complete.json`
- `weights/best.pt`
- `weights/last.pt`

Remote metrics khớp với metrics local.

---

## 7. Conclusions

1. Không nên thêm nhiều augmentation primitive mới ở thời điểm này. Kết quả cho thấy vấn đề chính là augmentation distribution và severity, không phải thiếu loại transform.
2. `viewport_mosaic_occlusion` có kết quả seed 42 tốt nhưng không ổn định ở seed 43–44. Không chọn làm recipe chính.
3. `mosaic_resolution_degrade_tail` là kết quả schedule tốt nhất theo mAP50-95 và AP75 trong nhóm late-tail, nhưng mAP50 vẫn dưới canonical baseline. Không đủ để thay thế canonical recipe.
4. `mosaic_occlusion` là candidate phụ đáng giữ lại vì multi-seed mean mAP50 gần baseline nhất, nhưng chưa có improvement có ý nghĩa.
5. Recommendation hiện tại: giữ **canonical GAP + FTAL với Mosaic → RandomPerspective** làm default; không tiếp tục mở rộng augmentation sweep nếu mục tiêu chính là tăng mAP50.
6. Các metric `AP50 small` và `tiny 1/2/3` chưa được evaluator hiện tại xuất ra, nên không được suy đoán hoặc điền vào báo cáo.
